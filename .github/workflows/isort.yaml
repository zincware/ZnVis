name: Check isort

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install isort
        run: |
          pip install isort
      - name: run isort
        run: |
          isort --check-only --quiet .
